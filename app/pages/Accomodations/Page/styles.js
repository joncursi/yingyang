/**
 * @flow
 * @prettier
 */

import css from 'styled-jsx/css';

import DIMS from '../../../constants/dims';

export default css`
  .contentContainer {
    padding: 0 ${DIMS.LAYOUT_PADDING}px;
    text-align: center;
  }
`;
